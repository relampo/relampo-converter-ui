---
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>Relampo Converter - Postman to YAML</title>
    <link rel="stylesheet" href="/assets/app.css" />
  </head>
  <body>
    <header>
      <div class="brand">
        <div class="logo" aria-hidden="true">
          <svg width="18" height="22" viewBox="0 0 18 22" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M10.5 0L0 12.5H7.5L6 22L18 9H10.5V0Z" fill="white" />
          </svg>
        </div>
        <div class="brand-text">
          <h1>RELAMPO</h1>
          <div class="subtitle">Postman to Recording YAML</div>
        </div>
      </div>
    </header>

    <div class="main-layout">
      <!-- Upload Panel -->
      <div class="panel">
        <h2 class="panel-title">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
            <polyline points="17 8 12 3 7 8"/>
            <line x1="12" y1="3" x2="12" y2="15"/>
          </svg>
          Upload Collection
        </h2>

        <div class="info-box">
          Convierte tu colección de Postman a formato YAML de Relampo. Esta versión es un placeholder local (sin backend).
        </div>

        <div class="upload-zone" id="uploadZone">
          <svg class="upload-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
            <polyline points="14 2 14 8 20 8"/>
          </svg>
          <div class="upload-title">Suelta tu archivo aquí</div>
          <div class="upload-subtitle">o haz clic para seleccionar</div>

          <div class="file-info" id="fileInfo">
            <div class="file-name" id="fileName">filename.json</div>
            <button class="file-remove" id="removeFile" title="Remove file">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="18" y1="6" x2="6" y2="18"/>
                <line x1="6" y1="6" x2="18" y2="18"/>
              </svg>
            </button>
          </div>
        </div>

        <div class="convert-actions">
          <button class="btn" id="convertBtn" disabled>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 3v12"/>
              <path d="m8 11 4 4 4-4"/>
            </svg>
            Convertir a YAML
          </button>
        </div>

        <input type="file" id="fileInput" accept=".json" />

        <!-- Validation Section -->
        <div class="validation-section" id="validationSection" style="display: none; margin-top: 24px;">
          <div class="validation-title">Validación de Estructura</div>
          <div id="validationResults"></div>
        </div>
      </div>

      <!-- Output Panel -->
      <div class="panel">
        <h2 class="panel-title">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
            <polyline points="14 2 14 8 20 8"/>
            <line x1="16" y1="13" x2="8" y2="13"/>
            <line x1="16" y1="17" x2="8" y2="17"/>
          </svg>
          YAML Output
        </h2>

        <textarea class="yaml-output empty" id="yamlOutput" placeholder="Tu YAML convertido aparecerá aquí..."></textarea>

        <div class="download-actions">
          <button class="btn btn-success" id="downloadBtn" disabled>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
              <polyline points="7 10 12 15 17 10"/>
              <line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
            Descargar YAML
          </button>
          <button class="btn btn-secondary" id="copyBtn" disabled>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="9" y="9" width="13" height="13" rx="2" ry="2"/>
              <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
            </svg>
            Copiar
          </button>
        </div>
      </div>

      <!-- Reference Panel -->
      <div class="panel reference-panel">
        <h2 class="panel-title">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <path d="M12 16v-4"/>
            <path d="M12 8h.01"/>
          </svg>
          Referencia
        </h2>

        <div class="reference-section">
          <div class="reference-title">Placeholder YAML</div>
          <div class="reference-code"># This is a placeholder output
test:
  name: "Recording..."
  version: "1.0"
scenarios:
  - name: "Scenario"
    steps: []</div>
        </div>

        <div class="reference-section">
          <div class="reference-title">✓ Se Genera (placeholder)</div>
          <ul class="reference-list">
            <li>metadata base</li>
            <li>json original en comentarios</li>
          </ul>
        </div>
      </div>
    </div>

    <!-- Toast -->
    <div class="toast" id="toast">
      <svg class="toast-icon" width="24" height="24" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <polyline points="20 6 9 17 4 12"/>
      </svg>
      <span class="toast-message" id="toastMessage">Success!</span>
    </div>
    <script type="module" src="/assets/app.js"></script>
  </body>
</html>
