# Relampo Postman Converter UI

Static single-page app for converting Postman collections to Relampo YAML.

## Run locally
Open `index.html` directly in your browser, or serve the folder with any static server.

Example using bun:
```sh
bunx serve .
```

Then visit the URL printed by `serve`.
